apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: dev-cluster
  description: >-
    Kubernetes v1.32.2
    | 1 CP + 0 Workers
    | Cilium
  annotations:
    backstaige.io/kubernetes-version: v1.32.2
    backstaige.io/cp-replicas: "1"
    backstaige.io/worker-replicas: "0"
    backstaige.io/cni: cilium
  tags:
    - docker
    - cilium
spec:
  type: kubernetes-cluster
  system: backstaige
  owner: infrastructure
  lifecycle: experimental
